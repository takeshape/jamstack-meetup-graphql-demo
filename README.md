# JAMStack Meetup GraphQL Demo

### Run the API
-  Open a new terminal tab run:
```
> cd api
> yarn install
> yarn start
```

### Run the Client
-  Open a new terminal tab run:
```
> cd client
> yarn install
> yarn start
```